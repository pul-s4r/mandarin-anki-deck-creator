# Documented flags/env for script-mode baseline (see tests/test_script_mode_baseline.py).
# Equivalent CLI: --cedict-path tests/baselines/cedict_sample.u8 --disable-sentences --no-skip-lines-filter
ANKI_PIPELINE_CEDICT_PATH=tests/baselines/cedict_sample.u8
